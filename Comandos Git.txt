Abrir la terminal en la carpeta del proyecto.

Solo si el repositorio es nuevo:

git init

Si no lo es continuamos:

git add . --> Para pasar al siguiente estado los cambios.
git commit -m "comentariodelcambio" --> Para guardarlo.
git push --> Para enviar el cambio a GitHub.